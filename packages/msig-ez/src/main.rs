// use msig::{api, multi};

pub fn main() {
    // let pubkey_json_file: &str = pubkey_json_file.unwrap_or();
    // let pubkey_json_file: &str = api::PUBKEY_JSON_FILEPATH;
    // let sig_signers: Vec<multi::MsigSigner> = api::load_pubkeys_json(pubkey_json_file).unwrap();
    // let res = api::add_signers_to_keyring(sig_signers.clone());
    // println!("{:#?}", res)

    // api::add_guard_cream_to_keyring().unwrap();
    // api::generate_msig(sig_signers, 2);
}
